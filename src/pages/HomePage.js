import { Container } from 'reactstrap';
import DiceRoller from '../features/diceroller/DiceRoller';

const HomePage = () => {
    return (
        <Container>
            <DiceRoller/>
        </Container>
    );
};

export default HomePage;